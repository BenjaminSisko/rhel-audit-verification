[state]
schema_version = <integer>

[<operation>]
snapshot = <JSON string>
# App-scoped setup backup, completion or restore record. No credentials.
# Managed by the setup wizard. Preserve these records during upgrades.
